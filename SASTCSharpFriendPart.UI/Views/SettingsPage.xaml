<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SASTCSharpFriendPart_UI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SASTCSharpFriendPart_UI.Controls"
    xmlns:vm="using:SASTCSharpFriendPart_UI.ViewModels"
    Background="Transparent">

    <Border Padding="36,40">
        <Grid MaxWidth="1200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="1">
                <StackPanel Spacing="16">
                    <TextBlock Text="外观"
                               FontSize="18"
                               FontWeight="SemiBold"/>

                    <!-- 主题选项 -->
                    <Border CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,8"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8"
                                        VerticalAlignment="Center">
                                <TextBlock Text="🎨"
                                           FontSize="20"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="应用主题"
                                           FontSize="16"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="选择应用亮色/暗色主题"
                                       FontSize="13"
                                       Opacity="0.9"/>
                            <ComboBox Width="200"
                                      Margin="0,8,0,0"
                                      ItemsSource="{x:Bind ViewModel.Themes, Mode=OneWay}"
                                      SelectedItem="{x:Bind ViewModel.SelectedTheme, Mode=TwoWay}"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="更多信息"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,16,0,8"/>

                    <!-- 帮助卡片 -->
                    <Border CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,8"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <Button HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                Command="{x:Bind ViewModel.HelpCommand}"
                                Background="Transparent"
                                BorderThickness="0">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8"
                                        VerticalAlignment="Center">
                                <TextBlock Text="❔"
                                           FontSize="20"
                                           VerticalAlignment="Center"/>
                                <StackPanel>
                                    <TextBlock Text="需要帮助？"
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="戳我了解更多->"
                                               FontSize="13"
                                               Opacity="0.9"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Page>
